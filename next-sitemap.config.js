/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://www.kumarfashion.com", // your live domain
  generateRobotsTxt: true, // also generate robots.txt
  sitemapSize: 7000, // split if too many URLs
};
